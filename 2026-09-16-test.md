{% assign lang = page.lang | default: "en" %}
{% assign ui = site.data.ui[lang] %}
{% if page.lang_alt_url %}
  {% assign alt_url = page.lang_alt_url %}
{% elsif lang == "en" %}
  {% assign alt_url = "/ru/" %}
{% else %}
  {% assign alt_url = "/" %}
{% endif %}
{% if lang == "ru" %}
  {% assign base = "/ru" %}
  {% assign status = site.current_status_ru | default: site.current_status_en %}
{% else %}
  {% assign base = "" %}
  {% assign status = site.current_status_en %}
{% endif %}
<header class="site-header">
  <div class="wrap">
    <section class="site-profile" aria-label="{{ ui.profile_aria }}">
      <div class="profile-banner">
        <picture>
          <source srcset="{{ '/assets/img/profile-banner.webp' | relative_url }}" type="image/webp">
          <img class="profile-banner-image" src="{{ '/assets/img/profile-banner.jpg' | relative_url }}" alt="" width="1983" height="793" decoding="async" fetchpriority="high">
        </picture>
        <div class="banner-grid"></div>
        <div class="magic-orbit magic-orbit-a"></div>
        <div class="magic-orbit magic-orbit-b"></div>
        <div class="profile-banner-copy">
          <span class="eyebrow">{{ ui.profile_eyebrow }}</span>
          <strong>{{ ui.profile_banner_title }}</strong>
          {% if ui.profile_banner_subtitle != empty %}<span>{{ ui.profile_banner_subtitle }}</span>{% endif %}
        </div>
      </div>

      <div class="profile-identity">
        <div class="profile-avatar-wrap">
          <picture>
            <source srcset="{{ '/assets/img/morui-avatar.webp' | relative_url }}" type="image/webp">
            <img class="profile-avatar" src="{{ '/assets/img/morui-avatar.jpg' | relative_url }}" alt="{{ ui.profile_alt }}" width="108" height="108" decoding="async" fetchpriority="high">
          </picture>
        </div>
        <div class="profile-name-block">
          <span class="profile-kicker">{{ ui.profile_kicker }}</span>
          {% assign home_url = base | append: '/' %}
          {% if page.url == home_url %}
            <h1 class="site-title">{{ ui.site_title }}<span class="site-title-sub">{{ ui.site_subtitle }}</span></h1>
          {% else %}
            <div class="site-title">{{ ui.site_title }}<span class="site-title-sub">{{ ui.site_subtitle }}</span></div>
          {% endif %}
          <p class="profile-role">{{ ui.tagline }}</p>
        </div>
        <div class="profile-sigil" aria-hidden="true">◎</div>
      </div>
    </section>

    <div class="site-header-meta">
      <div class="site-header-top">
        {% if status %}
        <p class="status-line"><span class="dot"></span><span class="label">{{ ui.status_label }}</span> {{ status }}</p>
        {% endif %}
        <a class="lang-switch" href="{{ alt_url | relative_url }}">{{ ui.lang_switch_label }}</a>
      </div>

      <nav class="site-nav" aria-label="{{ ui.nav_aria }}">
        <a href="{{ base | append: '/' | relative_url }}" {% if page.url == base | append: '/' %}class="is-active" aria-current="page"{% endif %}>{{ ui.nav_home }}</a>
        <a href="{{ base | append: '/projects/' | relative_url }}" {% if page.url == base | append: '/projects/' %}class="is-active" aria-current="page"{% endif %}>{{ ui.nav_projects }}</a>
        <a href="{{ base | append: '/team/' | relative_url }}" {% if page.url == base | append: '/team/' %}class="is-active" aria-current="page"{% endif %}>{{ ui.nav_team }}</a>
        <a href="{{ base | append: '/blog/' | relative_url }}" {% if page.url == base | append: '/blog/' %}class="is-active" aria-current="page"{% endif %}>{{ ui.nav_blog }}</a>
        <a href="{{ base | append: '/links/' | relative_url }}" {% if page.url == base | append: '/links/' %}class="is-active" aria-current="page"{% endif %}>{{ ui.nav_links }}</a>
        <a href="{{ base | append: '/archive/' | relative_url }}" {% if page.url == base | append: '/archive/' %}class="is-active" aria-current="page"{% endif %}>{{ ui.nav_archive }}</a>
        <a href="{{ base | append: '/cult/' | relative_url }}" {% if page.url == base | append: '/cult/' %}class="is-active" aria-current="page"{% endif %}>{{ ui.nav_cult }}</a>
      </nav>
    </div>
  </div>
</header>
