# Sitemap

Added `sitemap.xml` as a Jekyll template. GitHub Pages/Jekyll will generate the sitemap at:

`https://moruika.github.io/sitemap.xml`

It includes published pages and blog posts automatically, so new posts are picked up without manually editing the sitemap.
